import { call, put, takeLatest } from 'redux-saga/effects';
// import { loginAction } from './slice';

// function* loginSaga({ payload }: any) {
//   try {
//   } catch (error) {
//   }
// }

function* watchAdress() {
  // yield takeLatest(loginAction, loginSaga);
}

export default watchAdress;
